import 'package:flutter/material.dart';

ThemeData buildThemeApp() {
  final Color primaryColor = Color.fromRGBO(136, 14, 79, 1);

  return new ThemeData(
    primaryColor: primaryColor,
    fontFamily: 'Poppins',
  );
}
